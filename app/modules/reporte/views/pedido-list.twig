{% extends "@admin/base-list.twig" %}

{% block title %}
  Listado de pedidos
{% endblock %}

{% block header %}
  <h1>Listado de pedidos</h1>

{% endblock %}

{% block ctrls %}
  {% include "@admin/controls/buttons.twig" with { href_add:"reporte.php" } %}
{% endblock %}

{% block root %}
  {% if pedidos %}
    <table id="table-model" class="table table-striped table-bordered" cellspacing="0" width="100%">
      <thead class="thead-dark">
      <tr>
        <th style="width: 75px"></th>
        <th>Cod</th>
        <th>Fecha</th>
        <th>.Bs</th>
        <th>Estado</th>
        <th>Cliente</th>
      </tr>
      </thead>
      <tbody>
      {% for pedido in pedidos %}
        <tr>
          <td>
            pedido.php?option=find&id={{ pedido.id }},#
          </td>
          <td>{{ pedido.codigo }}</td>
          <td>{{ pedido.fecha }}</td>
          <td>{{ pedido.monto }}</td>
          <td>{{ pedido.estado }}</td>
          <td>{{ pedido.id_cliente }}</td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  {% else %}
    <h2>Sin datos</h2>
  {% endif %}
{% endblock %}